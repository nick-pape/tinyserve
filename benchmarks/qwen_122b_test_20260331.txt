=== Qwen 3.5-122B GGUF Q4_K Test ===

Shards: 3, total 71.7 GB
Architecture: qwen3_5_moe (multimodal wrapper + linear_attention + MoE)
  48 layers, 256 experts/layer, top_k=8
  hidden=3072, expert_intermediate=1024, head_dim=256
  Expert quant: Q4_K (not Q2_K as previously misreported)
  Expert tensors: fused format [out, in, 256]

Status: BLOCKED on model integration
  - GGUF parsing: WORKS (0.2s, all 879 tensors found)
  - Q4_K expert tensors: IDENTIFIED (144 fused tensors across shards)
  - Config extraction: WORKS (from HF text_config)
  - Model creation: FAILS — Qwen3_5MoeForConditionalGeneration is multimodal,
    load_from_gguf uses AutoModelForCausalLM which doesn't handle the wrapper.
    Config nesting (text_config) differs from standard MoE config layout.
    Field names differ (num_experts vs num_local_experts).

What's needed for inference:
  1. Use Qwen3_5MoeForCausalLM (text-only) instead of the conditional wrapper
  2. Map config field names (num_experts → num_local_experts)
  3. Handle fused expert format [out, in, 256] → per-expert slicing
  4. Linear attention layers (not standard attention) need separate handling
  5. 64 GB expert data with 49 GB RAM → disk offload required

No performance numbers available.
